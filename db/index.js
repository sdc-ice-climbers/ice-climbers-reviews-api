const { pool } = require('pg');

const pool = new Pool({
  host: ,
  port: ,
  user: ,
  password: ,
  database:
})


const Pool = require('pg').Pool
const pool = new Pool({
  user: 'austinchapin',
  host: 'localhost',
  database: 'ratings_reviews',
  port: 5432
})